{
    "version": 2,
    "builds": [
      {
        "src": "dist/server.ts",
        "use": "@vercel/node",
        "config": {"includeFiles": ["prisma/schema.prisma"]}
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "dist/server.ts"
      }
    ]
}