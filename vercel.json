{
  "version": 2,
  "builds": [
    {
      "src": "dist/server.js",
      "use": "@vercel/node",
      "config": { "includeFiles": ["prisma/**/*", "node_modules/.prisma/**/*"] }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/server.ts"
    }
  ]
}
